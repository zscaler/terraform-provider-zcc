name: ZCC Test
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # For systems with an upstream API that could drift unexpectedly (like most SaaS systems, etc.),
  # we recommend testing at a regular interval not necessarily tied to code changes. This will
  # ensure you are alerted to something breaking due to an API change, even if the code did not
  # change.

  schedule:
    - cron: '0 12 * * 1-5' # UTC
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  zcc-zsbeta-tenants:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goVersion: ["1.25"]
        environment:
          - ZCC_ZSBETA_TENANT01
    environment: ${{ matrix.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
              token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.goVersion }}

      - name: Clean existing Go modules
        run: go clean -modcache

      - name: Cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Setup Go Tools
        run: make tools

      - name: Download Go Dependencies
        run: |
          go mod tidy && go mod vendor

      - name: Setup Go Tools
        run: make tools

      - name: Check Formatting
        run: make fmtcheck

      - name: Lint Code
        run: make lint

      - name: Check Build
        run: make build

      - name: Run tests with retry
        uses: nick-fields/retry@v4
        with:
          max_attempts: 1
          # timeout_minutes is the wall-clock cap for this CI step.
          # It must exceed the -timeout flag baked into the
          # `test:integration:zcc` make target (currently 120m) plus
          # tooling overhead, otherwise the retry action kills the
          # job before `go test` would.
          timeout_minutes: 130
          command: |
            make test:integration:zcc
        env:
          ZSCALER_CLIENT_ID: ${{ secrets.ZSCALER_CLIENT_ID }}
          ZSCALER_CLIENT_SECRET: ${{ secrets.ZSCALER_CLIENT_SECRET }}
          ZSCALER_VANITY_DOMAIN: ${{ secrets.ZSCALER_VANITY_DOMAIN }}
          ZSCALER_CLOUD: ${{ secrets.ZSCALER_CLOUD }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TF_ACC: ${{ secrets.TF_ACC }}